# Codex Instructions — NexArtWO

Use `AGENTS.md` as the main instruction source.

Every task must be isolated.

Do not:
- modify protected files
- run SQL
- change Supabase config
- change financial formulas
- enable locked modules
- merge automatically

Before changes, produce:
- file list
- plan
- validation steps
- rollback plan
