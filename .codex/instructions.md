# Codex Instructions - NexArtWO

Use `AGENTS.md` as the universal instruction source. At a new task or context reset, read its five core files and then only the matching task-type row.

Before editing, identify exact files/functions, risks, validation, and rollback. Re-read context when scope, branch, phase, or protected operations change.

Do not load `Otros/` by default. Do not run SQL, change remote Supabase state or formulas, modify Investor Hub, commit, push, merge, or deploy without the exact gate required by `AGENTS.md`.
